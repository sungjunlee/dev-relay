---
milestone: Agentic Patterns Phase 0 — Wire What Exists
status: active
started: 2026-04-12
due: TBD
---

# agentic-patterns-phase-0

## Goal
Rubric, probe signals, and reliability analytics flow end-to-end through relay-plan → dispatch → review so later phases stop building on unwired artifacts.

## Plan

### Batch 1 — Foundation (sequential, #138 blocks #139 and #140)
- [x] #138 relay-plan/dispatch: make anchor.rubric_path mandatory and visible end-to-end → PR #147 (merged)

### Batch 1.5 — Phase 0.1 hardening (NEW, surfaced by post-merge adversarial review of #147)
Required before Batch 2: Phase 0.2/0.3 consumers assume rubric persistence is real. Currently only the manifest string is enforced — the file itself can be missing or point outside the run dir. Sequential, separate PRs (different modules, tighter review surface).
- [x] #148 Validate `anchor.rubric_path`: file exists + path contained within run dir → PR #155 (merged)
- [x] #149 Tighten manifest resolution: require `pr_number` match, reject stale/closed manifests → PR #164 (merged)
- [x] #156 Validate `run_id` as a single path segment at every consumer boundary → PR #159 (merged)
- [x] #157 review-runner fails open on non-loaded rubric states: PASS still advances to ready_to_merge → PR #162 (merged)
- [x] #165 Stale escalated manifests with pr_number:null still inheritable via branch reuse (BLOCKER, surfaced by codex post-merge re-review of #164) → PR #167 (merged)
- [x] #168 Branch-only fallback accepts ANY non-terminal+null pr_number, not just escalated (BLOCKER, surfaced by codex post-merge challenge of #167; state-machine-axis whitelist fix) → PR #169 (merged)
- [x] #170 filterByPr is state-blind — terminal manifests with matching stored pr_number can shadow fresh dispatched runs (HIGH defensive, NEW SIBLING AXIS, surfaced by codex post-merge challenge of #169; user picked Option A: continue ladder) → PR #173 (merged)
- [x] **#174 Resolver hardening epic — close the iteration-ladder pattern (E-Axis + R-Axis) → PR #175 (merged)**
- [x] #171 Recovery-message path emits unvalidated run_id → absorbed into #174 (PR #175 merged)
- [x] #172 Recovery-message close-run command refuses non-enum state values → absorbed into #174 (PR #175 merged)
- [x] **#177 Fail-closed state validation at resolver selectors (iteration-6 HIGH class — tampered/missing state bypassed #174's blacklist-inversion exclusion) → PR #178 (merged)**
- [x] #176 cleanup-worktrees.js emits raw manifest run_id into close-run command string (MED, surfaced by post-merge codex challenge of #175; filed out-of-scope of #177) → PR #183 (merged as `26c58fa`)
- [x] #166 gate-check pr_number stamping not concurrency-safe: duplicate `pr_number_stamped` events (MED, `phase-0-follow-up`, filed 2026-04-12 from codex post-merge challenge of #165) → PR #184 (merged as `c450588`)
- [~] #185 gate-check stamp-lock timeout allows merge on unstamped manifest (HIGH merge-safety, `phase-0-follow-up`, filed 2026-04-15 from codex post-merge challenge of #166/`c450588` probe 4)

### Batch 1.6 — Immediate post-#185 hardening / recovery follow-ups
Choose the next PR by implementation shape, not severity label alone. `#160` is the highest-severity remaining trust-root issue, but `#163` is the better next PR because it is narrower, high priority, and fixes a live operator recovery dead-end without opening a multi-module audit all at once.
- [ ] #163 rubric fail-closed recovery path is dead (`review_pending` cannot actually resume) → **NEXT PR**
- [ ] #160 validate `paths.repo_root` / `paths.worktree` as trust roots before any filesystem or GitHub side effect → **NEXT AFTER #163**
- [ ] #161 symlink rubric bypass (`path.resolve` / `statSync` follow links) → land after `#160` on the same trust-root surface
- [ ] #153 test fixtures default grandfathered — add enforcement-path / attack-surface coverage after `#160` / `#161` semantics settle
- [ ] #150 skip paths log rubric status in audit trail (visibility only; keep after runtime hardening)
- [ ] #151 grandfather flag → migration manifest + provenance (pair after `#163` so recovery-path semantics are stable first)
- [ ] #152 consistent repo-slug + path resolution (operator outage mode, not security-first)
- [ ] #158 run-id collision under concurrent dispatch (LOW-MED, same source as #156; correctness / future-proofing)

### Batch 2 — Planner consumption (unlocked by Batch 1.5; both touch `relay-plan/SKILL.md` — sequential)
**Re-sequenced 2026-04-14**: #177 (iteration-6 follow-up, fail-closed state validation) now gates #139 pending the post-merge codex challenge. Reason: the iteration-6 post-merge codex challenge of `501eb8e` (merged #174/PR #175) surfaced one HIGH class — #174's `BRANCH_ONLY_TERMINAL_STATES` literal blacklist is fail-open on tampered/missing state at the two EXCLUSION sites. User picked Option A (single narrow follow-up with explicit pattern break on the state-parsing axis; memory rule 7 adds "fail-closed state-validation meta-rule" that was missing from rule 4). #177 merged on 2026-04-14; #139 is BLOCKED on the post-merge codex challenge result until CLEAN.
- [x] #139 relay-plan: consume reliability-report output before rubric design → PR #179 (merged as `840c295`, 2 review rounds, 5/5 rubric factors PASS, all 5 AC verified)
- [x] #140 relay-plan: consume probe quality signals in rubric design → PR #180 (merged as `c126c42`, 2 review rounds, 5/5 rubric factors PASS, all 5 AC verified; Phase 0.3 closed, Phase 0 "Wire What Exists" complete)

### Batch 3 — Deferred design / process improvements
Execution order for remaining Phase 0 follow-ups now lives in Batch 1.6 above. Keep this section for items that are explicitly out of the immediate hardening / recovery sequence.

### Parking lot (separate timeline, not part of Phase 0)
- [ ] #154 Benchmark gstack-codex adversarial style into relay-review (IDEA; meta-process improvement)

## Running Context

### Design source
- `docs/agentic-patterns-adoption.md` — full reframed plan with Phase 0/1/2/3 structure
- Prior learning: `[rubric-lifecycle-gap]` (confidence 9/10, cross-project) warns against building enforcement on unwired artifacts
- Prior learning: `[wire-before-adopt]` (confidence 10/10, cross-model) captures this sprint's reframe

### Non-negotiables
- No new agentic patterns (Willison adoption) until this phase ships
- No autonomy scoring from probe signals — expose data, not inferred behavior
- Grandfathering required for #138 — see TODOS.md: "Rubric grandfathering for runs predating mandatory rubric_path"

### Sequencing constraint
- #138 must land first. It changes the manifest contract (`anchor.rubric_path` required for state transition). #139 and #140 extend planner behavior on top of that contract.
- Batch 2 items both edit `relay-plan/SKILL.md`. Plan to land them sequentially or merge into one PR if done together.
- **Updated 2026-04-12**: #156 and #157 (codex re-review findings on PR #155) are load-bearing on the same argument as #148: Phase 0.2/0.3 consumers assume the file-backed rubric invariant actually holds. Today the invariant is defeated at the trust root (`run_id` forgery, #156) and at the gate layer (review-runner fails open on non-loaded states, #157). Both should land before #139. #149 and #158 can land in parallel with them. Preferred order: `#149 || #156 || #157` (parallel, different modules) → #139.
- **Updated 2026-04-16**: remaining follow-ups split into two classes. `#160` is the highest-severity open trust-root issue, but `#163` should go first because it is a narrower high-priority recovery-path fix with a smaller review surface. Preferred order after `#185`: `#163 -> #160 -> #161 -> #153 -> (#150 || #151 || #152 || #158 as separate narrow PRs)`; only then resume Phase 1 backlog (`#141 -> #142`).

### Critical test coverage
- #138: regression test for grandfathered runs + regression test for rubric-missing rejection
- #142 (later Phase 1.2): integration test with real npm test must verify TDD prerequisite exclusion works (silent failure here wastes iterations)

## Progress
[Timestamped log — update at end of each session/batch]

- 2026-04-12 06:32: #138 dispatched (run `issue-138-20260412044608184`, rubric persisted at `anchor.rubric_path=rubric.yaml`) → PR #147 → reviewed 6 rounds (changes_requested x5, LGTM round 6) → merged (squash). Recursive dogfooding: #138's own PR was the first run to exercise the new mandatory-rubric enforcement end-to-end. Grandfathering implemented as Option A (explicit `anchor.rubric_grandfathered=true`, resume-only, blocked on new dispatches, operator-driven — no time cutoff).
- 2026-04-12 07:10: post-merge adversarial review (gstack-codex challenge mode, 1.23M tokens). Surfaced 9 substantive findings missed by the 6-round relay-review cycle: file-existence not validated (delete-after-dispatch bypass), path escape, branch-reuse manifest inheritance, skip-path rubric bypass, grandfather flag unauthenticated permanent, and test suite skewed toward happy-path. Round 4 + 5 explicitly flagged as compliance theater. Filed 7 follow-up issues.
- 2026-04-12 07:59: #148 dispatched (run `issue-148-20260412072649097`, rubric grade A at dispatch: 4 contract + 3 quality, 43% quality ratio, attack-surface factor included) → PR #155 → reviewed LGTM round 1 → merged (squash). Codex completed implementation in one iteration; all 7 rubric factors locked at ≥ 8/10. Grandfathered-run preservation confirmed via new tests. Anti-theater check verified each of the 4 regression tests fails against the pre-fix code (scoped per test in PR body). Attack-surface enumeration surfaced 4 new deferred vectors with no existing issue (symlink escape via `path.resolve` vs `fs.realpath`, non-empty-but-semantically-invalid rubric content, mid-review/mid-merge race, and `rubric_path` pointing at a directory/unreadable node) + 4 already-tracked (skip-path #150, grandfather hand-edit #151, repo-slug #152, Windows separators). File follow-up issues for the first 4 before Phase 0.2 starts consuming rubric content.
- 2026-04-12 09:44: post-merge adversarial re-review of PR #155 (gstack-codex challenge, 3.07M tokens). Relay-review round 1 LGTM was a **miss**: codex surfaced 4 bypass vectors the rubric factors didn't close. Filed: #156 (run_id trust boundary — containment resolves against attacker-controlled `run_id`, blocker), #157 (review-runner fails open on non-`loaded` rubric states — PASS + missing/empty/out-of-dir rubric still advances to ready_to_merge; `missing_path` warning is `null`, serious), #158 (run-id ms-timestamp collision under concurrent dispatch, minor). Finding #2 from codex (stale-branch manifest fallback) is already #149 scope — the #148 PR body attack-surface section should have acknowledged it as deferred but didn't (minor honesty gap in the enumeration factor). **Pattern**: the #138 compliance-theater failure mode re-emerged one layer up — #148's warnings are "visible" but not fail-closed. Sprint sequencing updated: #156 and #157 are load-bearing on the same argument that put #148 before #139, so they land before Phase 0.2 starts. Meta-lesson for rubric authoring: factor 3 wording was "surfaces missing rubric as visible warning, not silent null" — the "visible" word got optimized for, the "gate" semantics didn't. Future rubrics for security-sensitive invariants should explicitly name the enforcement layer (prompt / gate / transition) and fail-closed behavior as a separate factor.
- 2026-04-12 11:08: #156 dispatched (run `issue-156-20260412101412608`, rubric grade 6 factors: 4 contract + 2 quality with explicit enforcement-layer tags per the rubric-authoring meta-rule from `memory/feedback_rubric_fail_closed.md`) → PR #159 → codex reviewer (claude CLI unauthenticated on this host) → 3 relay-review rounds (round 1 caught `resolveRubricRunDir` falsy-early-return gap, round 2 caught `runRepoPath`/`repoPath` split regression introduced by round 1's fix, round 3 LGTM contract+quality PASS 0 issues) → squash-merged as `ce61bed`. The companion trust-root factor (factor 2) did its job — it caught the `resolveRubricRunDir` gap that a leaf-only factor would have missed. Validates the `memory/feedback_rubric_fail_closed.md` meta-rule.
- 2026-04-12 12:54: #157 dispatched (run `issue-157-20260412112541880`, rubric 6 factors explicitly split by enforcement layer per memory/feedback_rubric_fail_closed.md — [prompt layer] / [review-runner gate] / [gate-check gate] / [per-state×per-layer test coverage] / [operator communication] / [consumer audit]) → PR #162 → codex reviewer → 5 rounds → squash-merged as `d738ed8`. Rounds 1+2 fixed AC(f) happy-path regression. Round 3 caught a design bug (fail-closed state was ESCALATED = terminal-except-close, making recovery text unreachable — the same bug the orchestrator walked into mid-cycle when a no-op dispatch misclassification pushed the manifest to escalated; required manual manifest revert to recover). Round 4 switched fail-closed target to review_pending + next_action=repair_rubric_and_rerun_review + review.latest_verdict=rubric_state_failed_closed, keeping the recovery path live. Round 5 updated PR body matrix + added JSDoc documenting the recovery model.
- 2026-04-12 13:47: #149 dispatched (run `issue-149-20260412130914692`, rubric 6 factors applying all 3 accumulated meta-rules — enforcement-layer tagging from #155/#157 / sibling-field enumeration from #156/#160 / end-to-end recovery tests from #157/#163) → PR #164 → codex reviewer → 2 rounds (round 1 contract PASS + quality fail on missing consumer audit doc + historical manifest sampling; round 2 added docs/issue-149-manifest-resolution-audit-2026-04-12.md + 6 new integration tests across dispatch/close-run/update-manifest-state/finalize-run/review-runner + synthetic historical fixture → LGTM). Squash-merged as `955cd6e`.
- 2026-04-13 00:56: #165 dispatched (run `issue-165-20260412150826049`, rubric 5 factors M-size, narrow fix) → PR #167 → codex reviewer → 4 rounds (round 1 overcorrection on terminal-only classification; round 2 correctness fix; round 3 anti-theater scope comments; round 4 inline rationale + close-run header → LGTM) → squash-merged as `2e62895`. Closed the escalated+null case specifically.
- 2026-04-13 00:56: post-merge codex challenge on `2e62895` (1.16M tokens). **1 BLOCKER + 1 HIGH (same root cause). Codex second no-go signal for #139.** Filed **#168** — branch-only fallback accepts ANY non-terminal + null pr_number, not just escalated. Any manifest in `dispatched` / `review_pending` / `changes_requested` / `ready_to_merge` with `pr_number: null` is still inheritable by a new PR on the same branch. Also: ambiguity check runs on post-exclusion `branchFallbackMatches` set, so mixed-candidate cases silently pick the non-escalated candidate instead of surfacing ambiguity. **Meta-lesson extension**: sibling-enumeration meta-rule applies to the state-machine AXIS (every state at the enforcement point), not just trust-root fields. The "fix only the state cited in the bug" pattern is compliance theater — the legitimate null-pr case is only `dispatched`, so the right fix treats non-terminal as a whitelist (`dispatched` only), not a blacklist (merged/closed/escalated excluded). Memory updated at `feedback_rubric_fail_closed.md`.
- 2026-04-12 13:55: post-merge codex challenge on `955cd6e` (1.40M tokens). **1 BLOCKER + 1 LOW + 1 doc finding. Codex explicit no-go signal for #139.** Filed **#165** (BLOCKER — escalated manifests with pr_number:null still inheritable via branch reuse; the rubric's deliberate preservation of `escalated` as non-terminal to keep recovery open created a hole: branch-only fallback returns stale escalated manifests when pr_number is null, gate-check stamps the new PR onto the old manifest; this is the EXACT bypass #149 was supposed to close). Filed **#166** (LOW — concurrent gate-check stamping duplicates audit events). Doc overclaim in the new audit was finding 3 (low) but not filed separately. **Meta-lesson**: when preserving a state X as "recoverable", audit whether the recovery path uses the same resolver selector as the stale-inheritance path. If the same selector covers both, exclude X at that selector and require a different selector for legitimate recovery. Codex confirmed no #156/#157 regressions, historical manifests (#138/#148/#156/#157) all resolve correctly via both --run-id and matching --pr, close-run correctly rejects merged/closed, explicit --manifest and --run-id bypass ambiguity. Single-process stamping idempotence holds. But Batch 1.5 invariant "no stale-branch manifest inheritance" is NOT closed because of #165.
- 2026-04-12 13:03: post-merge codex challenge on `d738ed8` (1.13M tokens). 1 HIGH (new) + 1 MEDIUM (already #151). Filed **#163** — rubric fail-closed recovery path is dead: `dispatch.js:464` requires state=changes_requested but review-runner leaves manifest in review_pending, so the documented `dispatch --run-id` recovery is unreachable without manual manifest surgery. This is the SAME structural problem the orchestrator experienced mid-PR: fail-closed state + no supported resume transition = de facto terminal state. Round 3 codex review caught the ESCALATED variant; round 4 pivoted to review_pending but didn't widen dispatch.js's resume-source guard. #139 NOT blocked — the gate itself holds. **Deeper meta-lesson**: the "recovery path" factor in #157's rubric (factor 5, [operator communication]) required the path to be *named*, not *executable end-to-end*. Same compliance-theater pattern at a deeper layer: visible warning → fail-closed gate (closed in #155/#157) → recoverable state (closed in round 4) → *actually recoverable via real commands* (open, #163). Add to meta-rule: for operator-recovery factors, require an end-to-end test of the full flow, not just state reachability. Codex confirmed no reviewer-supplied-field bypass, no falling-through state, no PR-comment/manifest-state divergence, no #156 regression, and factor 6 matrix enumeration was complete. Grandfather manifest hand-edit bypass (medium) is already tracked as #151.
- 2026-04-12 11:18: post-merge codex challenge on `ce61bed` (1.89M tokens). 3 findings: (blocker) **#160** — `paths.repo_root` / `paths.worktree` is still an unvalidated trust root, crafted manifest with valid `run_id` can retarget dispatch/review/merge/cleanup at a different repo slug; same #148→#156 pattern one layer up (sibling fields, not leaf). (medium) **#161** — symlink rubric bypass still open (`path.resolve` vs `fs.realpath`); explicitly deferred in #148 PR body, now tracked. (medium) reliability-report dies on malformed run dir name — BLOCKS #139 Phase 0.2 consumption, fix within #139's scope not a separate ticket. Confirmed closed: `../victim-run` sibling-runDir bypass, `run_id=null`+grandfathered bypass, shape-regex bypasses, JSONL injection. **Meta-lesson extension**: when validating a trust root, enumerate all SIBLING fields in the same logical scope and name each one's trust status explicitly. The `paths.*` class was adjacent to `run_id` and escaped both #148's and #156's rubric audits. Neither #160 nor #161 blocks #139 per codex; #158 remains minor.
- 2026-04-12 23:15: #168 dispatched (run `issue-168-20260412231539071`, rubric 6 factors M-size applying ALL FOUR accumulated meta-rules — enforcement-layer tags / sibling-field enumeration / end-to-end recovery tests / state-machine-axis whitelist) → PR #169 → codex reviewer → 2 rounds (round 1 contract PASS + quality not_run on missing stale-candidate recovery generalization for non-escalated states; round 2 renamed `findStaleEscalatedBranchFallbackCandidate` → `findStaleNonTerminalBranchFallbackCandidate` and `buildEscalatedRecoveryMessage` → `buildStaleBranchFallbackRecoveryMessage` with state-specific `stale_${state}_run` reason strings, matrix + gate-check + end-to-end review_pending recovery tests updated accordingly → LGTM PASS, 0 issues, 6/6 factors pass) → squash-merged as `c8ebb01`.
- 2026-04-13 11:00: user decision (sprint orchestrator) on the codex no-go: **Option A — file #170 and keep iterating**. Filed **#170** (HIGH, NEW SIBLING AXIS — filterByPr state-blindness; will be the next iteration in the #149 → #165 → #168 → #170 ladder), **#171** (LOW — unvalidated run_id in recovery message), **#172** (LOW — #163-style recovery dead-end at resolver layer for non-enum state values). #170 explicitly takes the place of #168 as the next Batch 1.5 unblocker for #139. Sprint sequencing: #170 lands first; #171 + #172 can land in same sprint window or after #170 per the next codex challenge's findings.
- 2026-04-13 11:17: #170 dispatched (run `issue-170-20260413111740975`, rubric 6 factors M-size applying ALL FIVE accumulated meta-rules — enforcement-layer tags / sibling-field enumeration / end-to-end recovery test / state-machine-axis whitelist / **selector-composition axis enumeration** as the centerpiece) → PR #173 → codex reviewer → 2 rounds (round 1 contract FAIL on missing mixed-state ambiguity branch + missing review_pending sibling test cells; round 2 added the explicit `terminal exact-PR + fresh non-terminal sibling without matching PR` ambiguity branch at relay-resolver.js:241+ AND the missing review_pending/PR-miss test cells → LGTM PASS, 0 issues, 6/6 factors pass) → squash-merged as `1dee17a`. The selector-composition meta-rule worked as designed: the fix landed at the call site (`filterByPr(nonTerminalBranchMatches, prNumber)`), the four-selector audit table is now persisted at the top of relay-resolver.js, and per-selector inline state-awareness comments give every future iteration a static picture of where the discipline holds.
- 2026-04-13 12:01: user decision (sprint orchestrator) on the iteration-4 codex STOP signal: **Option C — file the resolver hardening epic #174 and re-sequence #139 after it**. Rationale: codex's literal verdict was "I would treat this as a stop-signal case, not another narrow ladder rung," and the cross-iteration STOP-signal meta-rule (compounding PR count at 4 iterations) explicitly requires user judgment here rather than auto-dispatch. Option C closes three new findings (E1 standalone `--pr` state-blindness HIGH, E2 mixed-state recovery message unactionable HIGH, E3 run_id leak MEDIUM) plus absorbs #171 and #172 under the single epic. Pattern break: the epic rubric MUST require call-site enumeration (not just function enumeration) AND recovery-message reachability tests (not just state non-terminality) to avoid iteration-6 surfacing a new scope. #171 and #172 remain filed as separate issues but are folded into #174's AC and will close when #174 merges. Memory updated at `feedback_rubric_fail_closed.md` (iteration-4 confirmation + scope-boundary trap note).
- 2026-04-14 12:30: user decision on iteration-6 STOP signal: **Option A — single narrow follow-up with explicit pattern break on the state-parsing axis**. Filed **#177** (fail-closed state validation at resolver selectors, BLOCKER for #139) and **#176** (cleanup-worktrees.js raw run_id leak, MED, separate side path tracked out-of-scope). Rubric 6 factors M-size: [contract] fail-closed whitelist, [contract] call-site enumeration + retry DETECTION carveout, [contract] invalid-state regression matrix 6-cell anti-theater, [contract] memory meta-rule 7 in the same diff, [quality] audit-table sync + docs mirror, [quality] consumer audit with `includeTerminal: true` preservation centerpiece.
- 2026-04-14 12:37: #177 dispatched (run `issue-177-20260414123724901`, codex executor) → PR #178 → codex reviewer → **4 rounds** (round 1: contract fail — inline meta-rule 7 rationale not carried to converted EXCLUSION helpers, A2/A3/B2/B3 tests missing anti-theater provenance comments, rule 7 memory entry missing `**Why:**`/`**How to apply:**` split and docs mirror not verbatim; round 2: contract PASS + quality fail on missing `#166/#163/#160/#161/#158/#151/#150/#152/#153` deferred-issue inventory in docs mirror; round 3: contract PASS + quality fail on stale audit-table line numbers + grep-output drift after round 2 added inline comments — same drift pattern as #174 round 4; round 4: **LGTM PASS**, all 6 factors pass, 0 issues, all 6 scope items verified) → squash-merged as `5362fc3`. Memory rule 7 ("fail-closed state-validation meta-rule") added in the same session; docs mirror `docs/issue-177-fail-closed-state-validation.md` quotes rule 7 verbatim, pins every call-site line number, and embeds the self-review grep output. Single-PR constraint held end-to-end. Process meta-lesson: the line-number drift artifact recurs whenever round-2 comments shift code below the audit table — same pattern as #174 round 4. Next time, require the round-1 rubric factor to include `grep -n` proof pinned to the FINAL post-fix tree, and have the executor regenerate audit-table numbers from source as the last edit in each round.
- 2026-04-15 13:41: post-merge codex adversarial challenge on `c450588` (10 probes scoped to concurrency-correctness invariants). **1 HIGH class found. 9/10 VERIFIED + 1 BYPASS-FOUND. Codex verdict: `HIGH-class-found`.** Probe 4 BYPASS: `stampPrNumberUnderLock()` timeout fall-through at `gate-check.js:137` returns the freshly re-read manifest even when `git.pr_number` is still null; `evaluateReviewGate` at `review-gate.js:113` never checks `manifestData.git.pr_number`; merge gate produced `{ status: "lgtm", readyToMerge: true }` on an unstamped manifest. Same compliance-theater pattern as #138/#155 one layer deeper: #166 split rule 1 correctly at the happy path (layer A + layer B) but kept a unified fail-safe timeout policy that inadvertently weakened the merge gate. Codex's in-memory repro on current source converged to `pr_number=null` / `lgtm` / `readyToMerge: true`. Other probes VERIFIED: concurrency convergence (5+ children → 1 event), disk-backed dedup, crash-write idempotence, non-terminal guard, sole-first-resolution claim, anti-theater regression tests, scope discipline, memory untouched, fail-safe/fail-closed documentation. **STOP rule applied per `memory/feedback_rubric_fail_closed.md` meta-rule 5**: did NOT auto-dispatch a follow-up iteration. Presented Options A/B/C to user. User picked Option A: filed **#185** (HIGH merge-safety) and started narrow fix cycle. Memory rule 8 (timeout/contention-fallthrough policy split) will be authored in the same PR as the fix per #177/PR #178 precedent. Output at `/tmp/codex-166-challenge.full.log` (5700 lines); prompt at `/tmp/codex-166-challenge-prompt.txt`; verdict summary at `/tmp/codex-166-challenge.out`.
- 2026-04-15 13:41: #185 filed as HIGH merge-safety follow-up; user decision **Option A** (tighten timeout to fail-closed within #166/#185 single-file scope). Dispatch in progress.
- 2026-04-15 13:19: #166 LGTM round 6 + merged as `c450588` via `finalize-run.js --merge-method squash`. Issue #166 CLOSED at 2026-04-15T13:19:48Z. Remote branch `issue-166` deleted; worktree cleaned. PR #184 closed **6 review rounds** — round 1 contract PASS on factors 1/2 + fail on factor 3 (anti-theater claim not reproducible in checked-in harness) + factor 4 (sibling audit rows overstated state-machine gating via validateTransition for cross-process concurrency); round 2 trimmed anti-theater claim + rewrote 6 UNCHANGED rationales, cleared factor 3 + most of factor 4, but `dispatch.js:817-823,968-975` row still claimed createRunId ms-uniqueness (exactly #158's concern); round 3 rewrote ONE audit-table cell to defer to #158, cleared factor 4, quality tier opened for the first time — factor F5 MED correctness finding that `stampPrNumberUnderLock` bypassed the caller's non-terminal filter (between resolveManifestRecord and lock acquisition, concurrent close/merge can transition the manifest; fresh re-read only checked pr_number); round 4 added inside-lock non-terminal guard via imported `isNonTerminalState` from relay-resolver (meta-rule 6 helper reuse) + shape-A terminal-state regression test — but widened relay-resolver.js API, breaking the rubric's F5 scope boundary (orchestrator misjudgment allowing the export in round-4 directive); round 5 docs-mirror legitimization (citing #176/PR #183 safeFormatRunId precedent) did NOT move the reviewer — F5 strictly enforces rubric-as-written; round 6 re-localized the whitelist inside gate-check.js via `STATES` from relay-manifest.js (already exported; no API expansion) + reverted resolver export + refreshed docs mirror to reflect the final scope — **LGTM PASS, both tiers PASS, all 6 factors green, 0 issues**. Four files shipped: `docs/issue-166-gate-check-stamping-concurrency.md` (+132, new docs mirror), `skills/relay-merge/scripts/gate-check.js` (+143/-21, stampPrNumberUnderLock + waitForPrNumberStampLock + readFreshManifestRecord helpers + local NON_TERMINAL_STATES_FOR_PR_STAMP whitelist + dedup), `skills/relay-merge/scripts/gate-check.test.js` (+187, 2 new regressions: 3-child cross-process concurrency test at :644+ and single-process terminal-state guard test at :746+). Test suite 324/324 (+2 new regression tests over the 323 baseline). Process note: 6 rounds is the highest round count since #138 (6 rounds pre-meta-rules) and #157 (5 rounds). Primary round-cost drivers: (a) round-1 anti-theater claim overstated; (b) round-1/2 sibling audit rows used `validateTransition` as a cross-process concurrency guard (it isn't); (c) round-3 correctness finding from the newly-opened quality tier — the extracted helper dropped a caller-side invariant (non-terminal guard); (d) round-4 orchestrator scope misjudgment on the resolver export; (e) round-5 docs legitimization did not convert the reviewer. Contract factors 1/2/3/4 cleared by round 3. Quality tier opened round 3; closed round 6. Single-PR constraint held end-to-end. Codex pushed each round's branch but did not open PR #184 (orchestrator opened manually in round 1 — same `gh pr create` sandbox block as prior PRs).
- 2026-04-15 12:09: #166 dispatched (run `issue-166-20260415120924250`, codex executor, branch `issue-166`). **Phase 0 follow-up #2 of 8; observation-window default per `memory/project_phase1_observation_gate.md`.** Rubric M-size 6 factors (4 contract + 2 quality), applying meta-rules 1/3/6/7 from `memory/feedback_rubric_fail_closed.md`. **Enforcement-layer split (rule 1) is load-bearing** — `writeManifest` at `relay-manifest.js:337-344` is ALREADY atomic via pid-suffixed tmp + renameSync (the field-write layer is correct); the bug lives SOLELY at `appendRunEvent`'s `fs.appendFileSync` at `relay-events.js:39` (no dedup). Rubric splits into two separate contract factors — [contract A] filesystem mutex (`fs.openSync(lockPath, "wx")` under `getRunDir(repoRoot, runId)`) wrapping the read-check-write-append sequence + bounded EEXIST wait + safe fall-through on timeout / [contract B] committed-journal dedup via `readRunEvents` pre-append check. [contract C] cross-process regression spawning 2-3 `gate-check.js` children via `spawn` + `Promise.all` against shared RELAY_HOME, asserting exactly one `pr_number_stamped` entry in events.jsonl. [contract D] call-site audit — 14-row table covering every `pr_number_stamped` emission + every manifest-stamping (discover-and-fill) path across relay-merge + relay-dispatch, with `gate-check.js:84-121` as the SOLE first-resolution stamping site claim; every UNCHANGED row cites `validateTransition` / state-machine gating / single-writer semantics. [quality E] scope guard + state-machine untouched + `pr_number is numeric; no trust-root sweep` note (distinct from #156/#160 path-interpolation discipline) + fail-safe-vs-fail-closed distinction (skip stamp branch cleanly under contention; distinct from #148/#155/#174/#177 security-refuse-to-proceed semantics). [quality F] docs mirror + rendered self-review grep + line-number drift guard. Dispatch prompt pins `tryResolveManifestForPr` at lines 84-121 + `appendRunEvent` call at 107-114 on `26c58fa`; provides a reference `stampPrNumberUnderLock` helper shape (not verbatim). Out-of-scope inventory repeated in both PR body and docs mirror: `#163`/`#160`/`#158`/`#161`/`#153`/`#152`/`#151`/`#150`/`#141`/`#142`. Background task id `b7p0hguj4`. Reliability probe pre-dispatch: 18 manifests (17+#176), median_rounds_to_ready=2, stale_open_runs_72h=0, round-cost-driver stuck factors in the 3-6 avg_rounds_to_met range all match known patterns (consumer-audit sibling-enumeration, end-to-end recovery); probe-executor-env `--project-only` returns empty `project_tools` (AC5 no-signal fallback as expected on dev-relay).
- 2026-04-15 11:43: post-merge codex adversarial challenge on `26c58fa` (8 probes scoped to containment invariants; no state-machine-axis reprobe per #177's iteration-ladder termination). **CLEAN. 8/8 VERIFIED. Zero findings. Codex verdict: `CLEAN`.** Probe results verbatim: (1) cleanup-worktrees.js:89 now computes runId via `safeFormatRunId({ manifestPath, data })`, :98 reuses it for closeCommand; parent `bd65f32` old lines 88/94 would emit `../victim-run`, new helper returns the basename fallback. (2) No uncovered raw-emission site — only fixed sites are :89/:98; unchanged but justified: :136 via appendRunEvent → ensureRunLayout → requireValidRunId, `close-run.js` :98/:106/:120 + `dispatch.js:454` via resolver validation at `relay-resolver.js:251`, `relay-resolver.js:51` as documented happy-path raw renderer, `reliability-report.js` aggregate/event-journal map-key sites only. Non-emission validator/comparator hits (`relay-manifest.js:396`, `relay-resolver.js:58/:252/:318`) do not reach operator output. (3) `safeFormatRunId` defined exactly once at `relay-resolver.js:57` and exported at :480-486; parent commit lacked the export; no duplicate helper in `skills/relay-dispatch/scripts/`. (4) Fallback stays defensive, not throwing — returns `validation.runId` or `formatManifestBasename(record)`; cleanup consumes before stale-open classification at :81 and prints at :169 so a tampered run_id does not abort enumeration. (5) Regression at `cleanup-worktrees.test.js:140` covers both JSON and text output, asserts tampered substring absent from runId/closeCommand/stdout at :171 and :185; against `bd65f32` those assertions would fail. (6) PR body + mirror explicitly defer `paths.repo_root`/`paths.worktree` to `#160`; diff contains no sibling trust-root hardening beyond deferral. (7) Docs mirror carries summary/audit/grep/out-of-scope/prior-art/round-discipline; no containment-critical review content exists only in PR body. (8) `memory/feedback_rubric_fail_closed.md` untouched by `bd65f32..26c58fa` — merge stays within #156/#174/#177 pattern rather than adding new meta-rule. Residual caveat: sandbox EPERM on `fs.mkdtempSync` blocks live test replay (orthogonal sandbox issue, same as #139/#140/#177 post-merge challenges); `node --check` passes for touched scripts; local `node --test` on orchestrator 322/322 green. Output saved at `/tmp/codex-176-challenge.full.log` (86 lines). **Phase 0 follow-up discipline held: no new meta-rule, no HIGH class, single review round, codex CLEAN on first challenge.** Memory not extended. Next-session default unchanged: `#166` (gate-check pr_number concurrency). Phase 1 observation gate: Phase 0 landed 2026-04-14; +2-week re-eval ~2026-04-28 (13 days remaining).
- 2026-04-15 11:37: #176 LGTM round 1 + merged as `26c58fa` via `finalize-run.js --merge-method squash`. Issue #176 CLOSED at 2026-04-15T11:37:37Z. Remote branch `issue-176` deleted; worktree cleaned. PR #183 closed **1 review round** — codex reviewer verdict `pass` on first try, contract + quality both PASS, 0 issues, all 6 rubric factors scored with explicit observed evidence (safeFormatRunId import at :25, single `runId` variable at :89, both emission sites refactored at :92/:98, tampered-manifest regression at `cleanup-worktrees.test.js:140-186`, docs mirror audit table cross-checked against current tree, sibling trust roots `paths.repo_root`/`paths.worktree` correctly deferred to #160). Four files shipped: `skills/relay-dispatch/scripts/cleanup-worktrees.js` (+5/-2), `skills/relay-dispatch/scripts/relay-resolver.js` (+1 — export added), `skills/relay-dispatch/scripts/cleanup-worktrees.test.js` (+51, new tampered-manifest regression), `docs/issue-176-cleanup-worktrees-raw-runid.md` (+106, new docs mirror). Process note: single-PR constraint held; codex pushed the branch but did not open PR (same `gh pr create` sandbox block as #139/#140/#174/#177 rd1) — orchestrator opened PR #183 manually. Executor completed in 590s (single iteration, 1 commit). Test suite 322/322 (+1 new regression test over the 321 baseline). Helper-reuse discipline from meta-rule 6 held end-to-end — `safeFormatRunId` was added to `relay-resolver.js` module.exports rather than duplicated inline.
- 2026-04-15 11:20: #176 dispatched (run `issue-176-20260415112039982`, codex executor, branch `issue-176`). **Phase 0 follow-up; observation-window default per `memory/project_phase1_observation_gate.md`.** Rubric M-size 6 factors (4 contract + 2 quality), applying meta-rules 1/3/6/7 from `memory/feedback_rubric_fail_closed.md`: [contract] sanitation at source (reuse `safeFormatRunId` at `relay-resolver.js:57-60`; export from the resolver — helper NOT currently in `module.exports`; forbid inline ternary duplication) / [contract] render-level sanitation (every operator-facing output channel — JSON `runId` + `closeCommand` fields + text lines :161/:165 — carries validated value; tampered `run_id` never surfaces) / [contract] end-to-end tampered-manifest regression test (fixture `run_id: "../victim-run"` + valid-basename filename; real-CLI invocation; assert JSON + text channels clean; anti-theater — must fail pre-fix) / [contract] call-site + sibling-script enumeration (audit table covers `cleanup-worktrees.js` :88/:94/:132, `close-run.js` :98/:106/:120, `dispatch.js` :454, `relay-resolver.js` :54/:58, `reliability-report.js` event-journal sites; each UNCHANGED row cites upstream validator — `validateManifestRecordRunId` / `requireValidRunId` at `ensureRunLayout`) / [quality] trust-root sibling discipline (`paths.repo_root` / `paths.worktree` deferred to `#160`; non-throwing fallback documented as defensive-by-design) / [quality] docs mirror + self-review grep + line-number drift guard. Prompt pins the `safeFormatRunId` reuse (no reinvention), the fail-closed-vs-defensive rationale for the non-throwing fallback, and a 10-row audit table with explicit Delta × Rationale columns. Out-of-scope inventory repeated in both PR body and docs mirror: `#160`/`#166`/`#163`/`#161`/`#158`/`#153`/`#152`/`#151`/`#150`/`#141`/`#142`. Background task id `bs2ngqjzh`. Reliability probe pre-dispatch (17 manifests, median_rounds_to_ready=2, zero stuck factors); probe-executor-env `--project-only` returns empty `project_tools` (no quality infra — AC5 no-signal fallback state as expected on dev-relay).
- 2026-04-14 23:34: **Phase 1 gate decision — honoring the adoption doc's 2-week empirical gate**. After merging #181 the orchestrator started planning #141 Rejection Log (Phase 1.1), then surfaced a planning conflict: `docs/agentic-patterns-adoption.md:206` explicitly conditions Phase 1 on "After Phase 0 lands + 2 weeks data." Phase 0 landed <24h ago. Observed pain patterns in last 2 weeks of iteration (line-number drift, docs-mirror staleness, compounding sibling-axis — #174 rd4, #177 rd3, #139 rd2, iteration ladder #149→...→#177) do NOT match #141's premise (factor-rejection-repeat) or #142's premise (test-shaping-around-implementation). Both Phase 1 items also modify core review/dispatch machinery on thin empirical ground — the exact `rubric-lifecycle-gap` pattern the adoption doc reframed against. **Decision**: defer #141 AND #142 until the 2-week observation window completes. During the window: (a) close observed Phase 0 defects (#176 MED, #166 MED); (b) consider tactical automation for observed drivers (line-number drift auto-regen; docs-mirror scaffolding). +2 weeks: re-read `reliability-report.js --json` output against real dispatch data; ship Phase 1 items only if the data shows the patterns they presume. Captured in `docs/agentic-patterns-adoption.md` Decision Log (2 entries dated 2026-04-14), in GitHub issue comments on #141 and #142, and in memory as a project-level gate. Next session's default is NOT "pick the next Phase 1 item" — it's "close #176, then #166, then reassess."
- 2026-04-14 23:07: #181 (LOW Phase 0.3 follow-up, docs contract drift) merged as `bd65f32` via direct commit + PR #182 (squash), no relay cycle (XS docs-only — 3-line SKILL.md edit). **Scope correction during implementation**: issue body claimed the `Error:` prefix stripping was probe-consumer-specific and that `reliability-report-consumer.js` did NOT strip, but verification against `c126c42` showed both consumers share an identical `formatFailureCause` helper that strips `Error:`. Picked Option B (doc-to-code) over Option A: updated SKILL.md § 1.5 lines 49-50 AND § 1.6 line 77 to add "(with any leading `Error:` prefix stripped)" to each "first stderr line" phrase; no code changes, no test changes, 321/321 green pre+post. CodeRabbit check passed. Issue #181 closed; scope correction comment posted on the GitHub issue for paper trail. **Phase 1 entry point decision**: #141 Rejection Log (Phase 1.1, S-size) before #142 TDD mode (Phase 1.2, M-size) per adoption doc sequence 1.1 → 1.2 + #142's "CRITICAL silent-failure here wastes an iteration" risk-matrix annotation warrants more Phase 0 operational data first. Rejection Log extends `formatPriorVerdictSummary()` in `skills/relay-review/scripts/review-runner.js:776` with per-factor structured rejection fields in `review-round-N-verdict.json`; consumer-axis pattern close to #139/#140 shape.
- 2026-04-14 15:29: post-merge codex challenge on `c126c42` (consumer-focused, 5 probes — no sibling-axis probes because #140 is a consumer-addition parallel to #139; iteration-ladder terminated at #177, #139 challenge already CLEAN). **1 LOW finding (documented follow-up, non-blocking). 4/5 VERIFIED, 1 PARTIAL on AC5 stderr-cause verbatim. Codex verdict on Phase 1 readiness: `GO with documented follow-up`.** Filed **#181** (LOW — `probe-executor-env-consumer.js:47` strips leading `Error:` prefix from stderr `<cause>`, contract-drift vs SKILL.md:77 which documents "first stderr line as `<cause>`"; NOT a rule-3 violation because the cause is still surfaced, just non-verbatim; the #139 `reliability-report-consumer.js` does NOT strip `Error:` so fixing the helper is the canonical option). Probe results verbatim: (1) METRIC COVERAGE — VERIFIED, SKILL.md Section 1.6 names `project_tools.frameworks` / `scripts` / `ci` + all 5 Quality Card fields with planning-use guidance; producer diff additive-only (new `scanCiWorkflows` + `project_tools.ci`, existing shapes unchanged). (2) FALLBACK REACHABILITY — BYPASS-FOUND (LOW, the #181 finding); no-signal / agent_probe_error / non-zero-with-stderr / non-zero-without-stderr / malformed-JSON paths all render through the shipped helper; live no-signal repro on dev-relay returned `project_tools: { scripts: [], frameworks: [], ci: [] }` and rendered `Probe signal: no quality infra detected.` exactly. (3) AC4 HARD GUARD — VERIFIED, three required phrases appear at SKILL.md:34 (#139 step) AND :60 (#140 step); AC4 denial phrase ("No autonomy scoring, no auto-calibration of rubric depth") at :60; grading + risk logic at :276-296 unchanged. (4) SCOPE CONTAINMENT — VERIFIED, `git diff --name-only 840c295..c126c42` touches only relay-plan files + docs/issue-140 mirror; cross-skill grep returned 0 matches; `reliability-report-consumer.*` diff empty. (5) PHASE 0 COMPLETION + AC — AC1/2/3/4 VERIFIED, AC5 PARTIAL on the verbatim-cause LOW; commit chain `80fa919` (#148) + `840c295` (#139) + `c126c42` (#140) corresponds to docs/agentic-patterns-adoption.md Phase 0.1/0.2/0.3; sprint file records the trilogy as landed. Residual LOW: codex sandbox mkdtemp EPERM blocks test harness execution (same as #139 post-merge); orchestrator ran `node --test skills/*/scripts/*.test.js` locally → all green. Output saved at `/tmp/codex-140-challenge.out`, full JSONL at `/tmp/codex-140-challenge.full.log` (81 lines), prompt at `/tmp/codex-140-challenge-prompt.txt`. **Phase 0 "Wire What Exists" structurally complete; Phase 1 unblocked pending orchestrator scheduling.**
- 2026-04-14 15:22: #140 LGTM + merged as `c126c42` via `finalize-run.js --merge-method squash`. Issue #140 CLOSED at 2026-04-14T15:22:15Z. Remote branch `issue-140` deleted; worktree cleaned. **Phase 0 "Wire What Exists" COMPLETE — all three Phase 0 issues (#148 rubric persistence + #139 reliability-report consumption + #140 probe consumption) have landed.** PR #180 closed 2 review rounds: round 1 contract PASS + quality FAIL on two narrow docs-mirror gaps (Consumer Audit Delta missing 4 adjacent-path rows + 3 deferred-scope bullets; line-number drift discipline rule not stated); round 2 `pass` — all 5 rubric factors scored, 0 issues, all 5 AC verified. Round 2 redispatch added 24 docs-only lines (4 unchanged-row entries + 3 deferred-scope bullets + `Line-number drift discipline` section). **Codex chose Path X** — extended `probe-executor-env.js` additively with `scanCiWorkflows` + `project_tools.ci` field (existing `project_tools.scripts`/`frameworks` shapes unchanged; 1 new CI-detection test on fixture). Six files shipped: `skills/relay-plan/SKILL.md` (+56/-4, new Section 1.6 + `Probe signal:` Quality Card block), `skills/relay-plan/scripts/probe-executor-env.js` (+21, additive CI scan), `skills/relay-plan/scripts/probe-executor-env.test.js` (+18, CI-detection test), `skills/relay-plan/scripts/probe-executor-env-consumer.js` (+192, new consumer helper), `skills/relay-plan/scripts/probe-executor-env-consumer.test.js` (+97, new, 3 separate failure-mode tests), `docs/issue-140-probe-signal-consumer.md` (+216 across both rounds, new docs mirror). Process note: single-PR constraint held end-to-end (no scope creep); codex pushed the branch but could not open the PR (same `gh pr create` sandbox block as #139 round 1) — orchestrator opened PR #180 manually. AC4 "no autonomy scoring, data exposure only" structural guard held — anti-autonomy grep returns zero non-denial matches. The three phrases carried over from #139 (`does not gate dispatch` / `does not alter state transitions` / `does not modify rubric structure`) prevented the paraphrase-drift trap that `changes_requested` round 1 of #139; round 1 of #140 passed contract tier on first try.
- 2026-04-14 14:55: #140 dispatched (run `issue-140-20260414145524055`, codex executor, branch `issue-140`). Rubric S-M 5 factors (3 contract + 2 quality) mirroring #139's consumer-axis shape: [contract] consumption step + data-exposure-only guard (AC1 + AC4, with three informational-only phrases verbatim + anti-autonomy grep guard) / [contract] Quality Card `probe_signal.*` named fields (≥ 5: test_infra / lint_format / type_check / ci / scripts; AC3 planning-use column mandated) / [contract] fallback reachability + optional CI-detection path (Path X extend `.github/workflows/` scan OR Path Y defer, either acceptable; 3 separate failure-mode tests per rule 3) / [quality] out-of-scope discipline + consumer audit (rules 6+7; Phase 0.3 completion framing) / [quality] worked-example pair + **synthetic-label guard** (dev-relay has no live infra, so populated example MUST be labeled SYNTHETIC; live probe is the no-signals case) + line-number drift guard. Insertion point pinned: new Section 1.6 between 1.5 (historical signal, #139) and Section 2 (rubric build). AC4 "no autonomy scoring" is the hard anchor — structural grep guard on `autonomy|auto-?calibrate`. Three phrases verbatim from #139 (`does not gate dispatch` / `does not alter state transitions` / `does not modify rubric structure`) carried over to enforce the non-gating boundary without paraphrase drift (the #139 round 1 reviewer lesson). Background task id pending completion. (consumer-focused, 5 probes — no sibling-axis probes because #139 is a consumer-addition, not a resolver fix; iteration-ladder terminated at #177). **CLEAN. 5/5 VERIFIED. Zero findings. Codex verdict on #140: `GO unconditional`.** Probe results verbatim: (1) METRIC COVERAGE — VERIFIED, all six producer fields named in SKILL.md (`factor_analysis.most_stuck_factor`, `factor_analysis.factors`, `rubric_insights.divergence_hotspots`, `rubric_insights.tier_effectiveness.contract.avg_rounds_to_met`, `rubric_insights.tier_effectiveness.quality.avg_rounds_to_met`, `metrics.median_rounds_to_ready`); producer schema unchanged vs `5362fc3`. (2) FALLBACK REACHABILITY — VERIFIED, codex directly reproduced all three failure modes in-memory via `readHistoricalSignal` and confirmed rendered output matches documentation: empty history → `status:"available"`, `empty_history:true` with per-field `no historical data available`; malformed-stderr → cause surfaced verbatim in "Reliability report unavailable: <cause>"; generic non-zero exits → first stderr line OR exit code surfaced with no swallowed cause. The shipped test file asserts the same three paths but could not run under codex sandbox (same `mkdtemp` EPERM that hit the reviewer during round 1); local `node --test skills/relay-plan/scripts/reliability-report-consumer.test.js` passes 3/3. (3) INFORMATIONAL-ONLY GUARD — VERIFIED, three required phrases verbatim at SKILL.md:34; grading table + risk signals still keyed only to rubric composition (SKILL.md:224, :237); helper extracts and renders only, no gating conditional keyed on report values. (4) SCOPE CONTAINMENT — VERIFIED, grep across sibling skills returned only the pre-existing `relay-dispatch/SKILL.md:148` command-index entry; no new consumers leaked; merge touched only 4 intended files. (5) AC VERIFICATION — VERIFIED for all 5 AC with pinned line numbers. Residual LOW: codex sandbox mkdtemp EPERM blocked test harness execution — orthogonal to #139, affects any PR with temp-directory-using tests. Output saved at `/tmp/codex-139-challenge.out`, full JSONL at `/tmp/codex-139-challenge.full.log` (87 lines), prompt at `/tmp/codex-139-challenge-prompt.txt`. #140 unblocked for dispatch.
- 2026-04-14 14:31: #139 LGTM + merged as `840c295` via `finalize-run.js --merge-method squash`. Issue #139 CLOSED at 2026-04-14T14:31:38Z. Remote branch `issue-139` deleted; worktree cleaned. PR #179 closed 2 review rounds: round 1 `changes_requested` on two narrow contract-wording gaps (informational-only guard phrasing not grep-exact + `factor_analysis.factors` path not named verbatim; quality tier deferred per review protocol since contract failed); round 2 `pass` — all 5 rubric factors scored, 0 issues, all 5 AC verified. Round 2 redispatch tightened SKILL.md:34 to contain the three required phrases verbatim (`does not gate dispatch`, `does not alter state transitions`, `does not modify rubric structure`) and amended the historical-signal table to name `factor_analysis.factors`; line-number drift regeneration updated the docs mirror's pinned references as the last edit of the round. Four files shipped: `skills/relay-plan/SKILL.md` (+68/-2), `skills/relay-plan/scripts/reliability-report-consumer.js` (+177, new), `skills/relay-plan/scripts/reliability-report-consumer.test.js` (+112, new, 3 separate failure-mode tests), `docs/issue-139-reliability-report-consumer.md` (+165, new). Process note: single-PR constraint held (no scope creep); codex pushed the branch but could not open the PR (`gh pr create` blocked in the executor sandbox) — orchestrator created PR #179 manually with the prepared body. Pre-existing `skills/relay-dispatch/SKILL.md:148` reference to `reliability-report.js` (a command-index listing, not a consumer) was correctly left untouched and documented in the docs mirror.
- 2026-04-14 13:58: #139 dispatched (run `issue-139-20260414135848915`, codex executor, branch `issue-139`). Rubric S-M 5 factors (3 contract + 2 quality): [contract] consumption step + informational-only guard / [contract] Quality Card `historical_signal.*` named fields / [contract] fallback reachability + failure-mode matrix (3 separate tests per rule 3) / [quality] out-of-scope discipline + consumer audit / [quality] worked-example pair + line-number drift guard. All 7 meta-rules applied; rules 3/6/7 directly load-bearing on the consumer axis, rules 1/4 indirectly via the AC5 "informational only" guard. Prompt pins the SKILL.md insertion point to `5362fc3`, lists the six `reliability-report.js` JSON fields to parse, enumerates the 3x failure-mode matrix with real test coverage, spells out the scope guard + deferred issues (#176/#166/#163/#160/#161/#158/#151/#150/#152/#153/#140), and requires a docs mirror at `docs/issue-139-reliability-report-consumer.md` with verbatim self-review grep + populated/no-history worked examples. Background task id `b7d81418i`.
- 2026-04-14 13:40: post-merge scoped codex challenge on `5362fc3` (2.21M tokens, 5 probes — 5-part Batch 1.5 invariant ONLY; sibling-axis probes excluded per iteration-6 pattern-break rule because this PR IS the sibling-axis closure). **CLEAN. 5/5 VERIFIED. Zero findings. Codex verdict on #139: `GO`.** Verbatim probe results: (1) rubric file exists + path contained: VERIFIED; (2) run_id validated everywhere in resolver: VERIFIED (`cleanup-worktrees.js` excluded per scope, tracked separately as #176); (3) reviewer fails closed on non-loaded rubric states: VERIFIED; (4) no stale-branch manifest inheritance including tampered/unknown states: VERIFIED — codex re-probed all 4 iteration-6 shapes (branch+PR with `state:'bogus'`, branch+PR with missing state, standalone `--pr` with `state:'bogus'`, standalone `--pr` with missing state) against the current resolver in-memory; all four now return `No relay manifest found ... invalid state` and fail-close instead of resolving a stale manifest. Valid controls preserved: dispatched exact-PR works, terminal-inclusive `includeTerminal:true` cleanup still works; (5) operator recovery reachable: VERIFIED — new invalid-state recovery text is command-free at relay-resolver.js:227; only command-bearing stale-run recovery remains guarded by `validateTransition(state, CLOSED)`. Output saved at `/tmp/codex-177-challenge.out`, full JSONL at `/tmp/codex-177-challenge.full.log` (105 lines), prompt at `/tmp/codex-177-challenge-prompt.txt`. The iteration ladder `#149 → #165 → #168 → #170 → #174 → #177` terminates at iteration 6 with zero new classes on the 5-part invariant. #139 and #140 are unblocked.
- 2026-04-13 13:53: post-merge codex challenge on `501eb8e` (1.89M tokens, 8 probes — 5-part Batch 1.5 invariant + call-site audit + recovery-message reachability + pattern-break verification). **2 findings: 1 HIGH (NEW CLASS, sixth iteration) + 1 MEDIUM (out-of-resolver scope). ITERATION-6 STOP SIGNAL TRIGGERED — one HIGH class threshold met per the explicit pattern-break rule.** Probe verdicts verbatim: (1) rubric file exists + path contained: VERIFIED. (2) run_id validated: BYPASS-FOUND (medium) — `cleanup-worktrees.js:88` and `:94` still echo raw `data.run_id` into suggested close-run commands; tampered `run_id="../victim-run"` leaks at this side path (probe 2 not fully closed end-to-end). (3) reviewer fails closed on non-loaded rubric states: VERIFIED. (4) no stale-branch manifest inheritance: BYPASS-FOUND (HIGH) — call sites `:351` (branch+PR composition with `nonTerminalBranchMatches`) and `:391` (standalone `--pr` via `filterOutTerminal`) are only state-aware for KNOWN terminal states. `BRANCH_ONLY_TERMINAL_STATES = {MERGED, CLOSED}` is a literal-match blacklist; a tampered manifest with `state: "bogus"` is NOT in the set, so `filterByBranch({excludeTerminal: true})` and `filterOutTerminal(allRecords)` both let it through. Repro: stale `bogus/pr=123` sibling + fresh `dispatched/pr=null` sibling on same branch → `resolveManifestRecord({branch, prNumber: 123})` AND `resolveManifestRecord({prNumber: 123})` both resolve the stale `bogus` record instead of fail-closing. The only invalid-state regression added in #175 is the stale-fallback path at `relay-resolver.test.js:1374`; no exact-PR/standalone-`--pr` coverage for the invalid-state axis. This reopens the E1 class under manifest tampering. (5) operator recovery reachable: VERIFIED. (6) all selectors at every call site state-aware: BYPASS-FOUND — 15/15 audited call sites enumerated, no fifth resolver-like helper outside `resolveManifestRecord`, but rows `:351` and `:391` are overstated because they ignore the invalid-state axis. (7) recovery-message reachability walk: VERIFIED — every `buildXxxError`/`buildXxxRecoveryMessage` emission is internally reachable and names commands the state machine accepts. (8) pattern-break verification: BYPASS-FOUND — iteration-6 HIGH class, invalid-state tampering defeats the new exact-PR terminal-exclusion logic and reopens stale-manifest shadowing. Sibling-axis: NEW FINDING (invalid `state` values opt stale terminals back into the non-terminal subsets). NO NEW CLASS on `includeTerminal` propagation, `filterOutTerminal` widening, audit-table mirror drift, or new branch+PR bypass. Output saved at `/tmp/codex-174-challenge.out` (36 lines), full log at `/tmp/codex-174-challenge.full.log` (119 lines), challenge prompt at `/tmp/codex-174-challenge-prompt.txt`. Tokens: 1,893,572. **Verdict: NO-GO for Phase 0.2 / #139.** Blocker: HIGH invalid-state exact-PR bypass. The pattern break did NOT work — the rubric required call-site enumeration (which held: 15/15 sites correctly enumerated), but the new state-aware filters use a BLACKLIST (`BRANCH_ONLY_TERMINAL_STATES`) instead of a WHITELIST (`KNOWN_NON_TERMINAL_STATES`), and the state-machine-axis whitelist meta-rule from `memory/feedback_rubric_fail_closed.md` (rule 4) was applied to STATE OF THE BUG, not to STATE PARSING ITSELF. Surfacing to user with Options A/B/C per iteration-6 STOP rule.
- 2026-04-13 13:40: #174 dispatched (run `issue-174-20260413121826503`, rubric 7 factors L-size applying ALL SIX accumulated meta-rules — enforcement-layer tags / sibling-field enumeration / end-to-end recovery test / state-machine-axis whitelist / selector-composition axis enumeration / **call-site extension** as the centerpiece per the iteration-4 scope-boundary trap). Codex executor; codex reviewer. **5 review rounds**: round 1 changes_requested (E1 fixed but `finalize-run --skip-merge --pr` cleanup workflow regressed; standalone `--pr` rejection lacked recovery text); round 2 changes_requested (added `includeTerminal` opt-in for `--skip-merge` cleanup + threaded candidates/recovery through `buildNoManifestError`, but audit table line numbers stale + standalone-`--pr` matrix incomplete + gate-check headRefName=null had no test); round 3 changes_requested (audit table line numbers fixed + 2 missing test cases + gate-check test added, but DRAFT state missing from close-run reachability loop + PR body not visible to reviewer); round 4 changes_requested (DRAFT added + `docs/issue-174-resolver-hardening.md` mirror created, but the round-4 cross-link comment shifted every audit-table line by +1 and docs file omitted the call-site table itself); round 5 LGTM PASS (manual realignment commit `3f1a0b4` fixed +1 drift and added the call-site table to the docs file; codex verified all 7 factors PASS, contract+quality both pass, no scope drift). Squash-merged as `501eb8e`. PR #175 closed #174/#171/#172. The call-site extension meta-rule held — the rubric required call-site enumeration in the audit table AND in the test matrix; both were enforced. Memory rule 6 (call-site extension) applied successfully on its first epic-scale exercise. Single-PR constraint held end-to-end (rounds 1+2+3+4+5 stayed in one PR; codex never suggested splitting). Process meta-lesson: relay-review's reviewer prompt does not include the PR body, so contract items requiring "PR body documents X" become structural failures unless mirrored into a tracked doc — captured in `docs/issue-174-resolver-hardening.md` pattern, file follow-up if review-runner should be enhanced (not in this PR per the ladder-break discipline).
- 2026-04-13 11:48: post-merge codex challenge on `1dee17a` (166k tokens, smaller scope this iteration — narrow diff). **3 findings: 1 HIGH (NEW CLASS, fifth iteration) + 1 HIGH (UX) + 1 MEDIUM (run_id leak in new code path). Codex fourth no-go signal for #139.** Five-part invariant verdict verbatim: (1) rubric file exists + path contained: **VERIFIED**; (2) run_id validated: **BYPASS-FOUND (medium)** — new mixed-state ambiguity branch at relay-resolver.js:245 → 261 formats raw stored run_ids before validation, same class as #171 but in a separate code path; (3) reviewer fails closed on non-loaded rubric states: **VERIFIED**; (4) no stale-branch manifest inheritance: **BYPASS-FOUND (high, fifth-iteration class)** — branch+PR exact-PR composition was fixed at relay-resolver.js:208, but the **standalone `--pr` selector at relay-resolver.js:237** (`filterByPr(allRecords, prNumber)`) is still state-blind. Reachable via `finalize-run --pr <n>` without `--branch` (also conditionally via gate-check.js:217 when `headRefName` is absent). Repro: `stale merged/pr=123` + `fresh ready_to_merge/pr=null` on same branch + `resolveManifestRecord({ repoRoot, prNumber: 123 })` returns the stale merged run. (5) operator recovery reachable: **BYPASS-FOUND (high, UX)** — new mixed-state ambiguity branch (`merged/pr=match` + `review_pending/pr=null`) throws the generic ambiguity message suggesting `--run-id` / `close-run`, but neither command is reachable: close-run rejects terminal manifests, and dispatch --run-id rejects review_pending (only resumes changes_requested). Sixth probe (selector-composition audit): **BYPASS-FOUND** — the audit table at relay-resolver.js:1 claims `filterByPr` is state-aware via composition, but lines 237 and 245 use it directly without composition. Sibling-axis: NEW FINDING for the recovery-message precedence error; NEW FINDING for the new ambiguity-path metadata leak; NO NEW CLASS for #166 concurrent stamping; NO NEW CLASS for fifth resolver-like helper outside resolveManifestRecord. Output saved at `/tmp/codex-170-challenge.out` (33 lines), full log at `/tmp/codex-170-challenge.full.log` (8488 lines), challenge prompt at `/tmp/codex-170-challenge-prompt.txt`. Tokens: 166,424. **CROSS-ITERATION STOP SIGNAL TRIGGERED**: this is iteration 5 in the chain (#149 → #165 → #168 → #170 → ?). The fifth-iteration class (standalone `--pr` state-blindness) was hidden by the rubric's scope ("compose `filterByPr` with `nonTerminalBranchMatches` at the branch+PR call site" — branch+PR scope only, didn't audit standalone `--pr`). Per the cross-iteration STOP-signal meta-rule from `memory/feedback_rubric_fail_closed.md`, compounding PR count is a separate risk signal that requires user judgment. Surfacing to user with Options A/B/C rather than auto-dispatching iteration 5.
- 2026-04-12 23:53: post-merge codex challenge on `c8ebb01` (1.01M tokens). **1 HIGH + 2 LOW. Codex third no-go signal for #139.** Per task rules (compounding PR count is a risk signal; NEW-class bypass requires user input), STOPPING and surfacing to user rather than auto-dispatching a fourth iteration. Five-part invariant verdict verbatim: (1) rubric file exists + path contained: **VERIFIED** (gate-check + review-gate coverage intact, no new bypass from #168); (2) run_id validated: **BYPASS-FOUND (low)** — `buildStaleBranchFallbackRecoveryMessage` uses raw `record?.data?.run_id` via `formatRunId` before `validateManifestRecordRunId` ever runs, so tampered manifests can emit `--run-id "../victim"` in operator-facing error text; JSON.stringify prevents shell injection, but the containment invariant from #156 is inconsistent at this code path; (3) reviewer fails closed on non-loaded rubric states: **VERIFIED** (fixture change didn't hide fail-closed logic — helpers still resolve via --run-id not branch fallback); (4) no stale-branch manifest inheritance: **BYPASS-FOUND (high, NEW SIBLING AXIS)** — `filterByPr(branchMatches, prNumber)` at relay-resolver.js:182 operates on `filterByBranch(records, branch)` which does NOT exclude terminal, so a stale `merged`/`closed` manifest with matching stored pr_number can still shadow a fresh `dispatched+null` run on the same branch. Not exploitable in normal flow (GitHub PR numbers don't naturally collide with prior merged PRs), but is a defensive gap under manifest tampering or historical pre-#168 stamping corruption; #149's terminal-exclusion discipline was never extended to the exact-PR selector path; (5) operator recovery reachable: **BYPASS-FOUND (low)** — tampered manifest with `state: "bogus"` gets a recovery message directing operator to `close-run.js --reason "stale_bogus_run"`, but close-run's `updateManifestState` rejects unknown FROM states; also same-run `dispatch --run-id` doesn't resume `closed` manifests so post-close recovery requires fresh dispatch not same-run resume (same class as #163 but one layer up). Sibling-axis probes: (a) filterByPr state-blindness = NEW FINDING (selector-composition axis, not state-machine-state axis — genuinely new class per task rules); (b) filterByBranch with excludeTerminal:false consumer = NEW FINDING (same root cause as filterByPr finding — terminal branch matches are passed into state-blind selectors downstream); (c) recovery-message info leak via gate-check.js machine-readable output = NEW FINDING (low, log-injection not shell-injection); (d) N>2 pre-whitelist ambiguity with 1 whitelisted + N-1 stale = NO NEW CLASS (generic ambiguity error is UX-only, fail-closed behavior is correct). Output saved at `/tmp/codex-168-challenge.out` (78 lines) and `/tmp/codex-168-challenge.jsonl` (full JSONL trace). Tokens: 1,008,824. Test execution blocked by sandbox tempdir denial; analysis is code-inspection only but the core claim (`filterByPr` sees terminal matches) is directly verifiable in the diff (line 175 of the merged resolver uses `filterByBranch(allRecords, branch)` without `excludeTerminal`). **STOP signal triggered**: 3 consecutive codex challenges (`d738ed8` → `955cd6e` → `2e62895` → `c8ebb01`) have each found a new-class bypass at one layer up. Meta-pattern confirmed: rubric-enforced invariants are a moving target because each layer's fix exposes the adjacent layer's weakness. User should decide scope — see final go/no-go assessment in this session's output.
- 2026-04-16 21:38: triaged remaining open follow-ups after the `#185`/PR #186 pass. Chosen execution order: `#163 -> #160 -> #161 -> #153 -> (#150 || #151 || #152 || #158)`; only then resume Phase 1 backlog (`#141 -> #142`). Rationale: severity-only top remains `#160` (trust-root retargeting across dispatch/review/merge/cleanup), but the best next PR is `#163` because it is a narrower high-priority recovery-path contradiction between `review-runner` and `dispatch --run-id`, making it easier to review and safer to land before the larger trust-root sweep.
- 2026-04-16 21:53: compressed the post-Phase-0 simplification / eng-review backlog into [docs/runtime-cleanup-backlog-2026-04-16.md](../../docs/runtime-cleanup-backlog-2026-04-16.md). Kept it out of `backlog/tasks/` on purpose because `tasks/` is a thin GitHub mirror and these items are still draft planning, not synced issues. Shape locked as: one prerequisite bucket (`#185`, `#163`, `#160`, `#161`, `#151`) + two future epics: `Runtime Boundary Cleanup` (worktree consolidation, manifest boundary split, review-runner decomposition) and `Lifecycle Migration Cleanup` (grandfathering retirement, resolver/docs hygiene).
- 2026-04-16 22:00: registered the compressed cleanup backlog in GitHub. New issues: Epic A `#192`, Epic B `#193`, worktree consolidation `#187`, relay-manifest boundary split `#188`, review-runner decomposition `#189`, grandfathering retirement `#190`, resolver/docs + CLI hygiene `#191`. Local backlog doc updated to mirror the issued numbers instead of leaving A1/B1 placeholders only.

### Backlog follow-ups from #138 (tracked as GitHub issues)
- #148 **high**: Validate `anchor.rubric_path` — file existence + run-dir path containment. Codex findings #1, #2.
- #149 **high**: Tighten manifest resolution — require `pr_number` match, reject stale/closed manifests. Codex finding #3.
- #150 med: Skip paths (`--skip` / `--skip-review`) should log rubric status in audit trail. Codex finding #4.
- #151 med: Grandfather flag redesign — replace per-manifest bool with migration manifest + provenance. Codex findings #5, #6, #8.
- #152 low-med: Consistent repo-slug + path resolution across relay entry points. Codex findings #7, #9. (Supersedes the original review-runner slug observation from round 1.)
- #153 med: Test fixtures default to grandfathered mode — add attack-surface coverage. Codex tests-as-theater critique.
- #154 enhancement: Benchmark gstack-codex adversarial style into relay-review. Meta issue.

**Sequencing recommendation for Phase 0.2 (#139) and Phase 0.3 (#140)**: #148 and #149 should land before adding new consumers (reliability-report, probe signals). Those consumers assume rubric is truly persisted, which is currently only a string-level invariant. #150, #151, #152, #153, #154 are non-blocking.
