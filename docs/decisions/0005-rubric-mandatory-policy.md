# ADR-0005: Rubric Mandatory — Grandfathering Retired

Status: Superseded as a module record. The durable rule remains: a new run
requires frozen Done Criteria (or a rubric used as that anchor); there is no
grandfather bypass. Current contract:
[architecture.md](../../references/architecture.md).

The `anchor.rubric_grandfathered` flag, `manifest/rubric.js`, and
`close-run.js` named in the original record no longer exist. Do not restore
them. The original module write-up (`#190`) lives in git history.
